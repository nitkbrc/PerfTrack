<%
  frame_id = request.headers["Turbo-Frame"].presence_in(%w[notifications notifications_mobile]) || "notifications"
%>
<%= render "shared/notification_bell",
      notifications: @notifications,
      unread_count: @unread_count,
      open: params[:open].present?,
      frame_id: frame_id %>
