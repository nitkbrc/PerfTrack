<% full = intensity.to_sym == :full %>
<div class="pointer-events-none fixed inset-0 -z-10 overflow-hidden bg-[#1B4332]"
     data-controller="campus-rotator"
     data-campus-rotator-full-value="<%= full %>">
  <% campus_image_paths.each_with_index do |path, index| %>
    <%= image_tag path, data: { campus_rotator_target: "image" },
          class: "absolute inset-0 h-full w-full object-cover transition-opacity duration-1000 #{index.zero? ? '' : 'opacity-0'}",
          alt: "" %>
  <% end %>
  <% if full %>
    <div class="absolute inset-0 bg-[#1B4332]/55"></div>
  <% else %>
    <%# Forest wash first, then soft cream — green reads through without crushing the photo. %>
    <div class="absolute inset-0 bg-[#1B4332]/28"></div>
    <div class="absolute inset-0 bg-[#F7FAF7]/68"></div>
  <% end %>
</div>
